# AI Station × 壽司博士 Dr. Source — 正式提案卡

> 提案版本：v1.0　｜　日期：2026-07-26　｜　提案方：AI Station 開發團隊
> 對象：創價未來｜壽司博士 Dr. Source（主持人 楊坤修博士 / 善向永續 ESG Sunshine）

---

## 0. 一句話提案

**把「寫一段腳本」到「出一支帶品牌開場、逐字字幕、可立即分發的影片」之間的七道工序，
壓縮成一條全自動生產線——預設零雲端成本，需要更高品質時再插上雲端金鑰。**

AI Station 已為壽司博士內建品牌預設（頻道憲法 v1.0），從開場 slate、調色盤、腳本 DNA
到 AI 邊界，全部寫死在程式裡，每次生成都不必重新解釋品牌。

---

## 1. 我們要解決的問題

| 痛點 | 現狀 | AI Station 的解法 |
|------|------|-------------------|
| 出片慢、靠人工剪輯 | 一支片從腳本到成片需數小時~數天 | 腳本貼入 → 自動出片，平均 ~20 秒/支（本地） |
| 品牌一致性靠人盯 | 不同剪輯手感不一，開場/配色常跑掉 | 品牌預設強制：深藍→暖金開場、DNA 調色盤、固定片頭台詞 |
| 字幕是沉重手工 | 逐字對齊耗時且易錯 | 語音邊界自動對齊，卡拉 OK 式逐字燒入 |
| 雲端成本不確定 | 一開就擔心帳單 | 預設走 edge-tts + ffmpeg + Pillow，0 元；金鑰才升級 |
| 難以規模化 / 排程 | 一支一支手做 | n8n webhook + 定時觸發，可批次 / 排程生產 |

---

## 2. 解決方案：7 模組生產線（對應頻道規劃書 IDEA 架構）

| # | 工序 | 預設（免費） | 雲端增強（選用） |
|---|------|--------------|------------------|
| 1 | 編排中心 | FastAPI + 背景執行緒池，提交即回 `queued` | — |
| 2 | 文字解析（LLM 腦） | 內建句法解析 + 壽司博士 DNA 標記 | OpenAI GPT-4o 鏡頭規劃 |
| 3 | 語音合成 | edge-tts（多語、免費） | ElevenLabs |
| 4 | 視覺生成 | Pillow 品牌漸層（自動套色） | Runway B-roll |
| 5 | 渲染引擎 | ffmpeg + 同步字幕 | — |
| 6 | 雲端儲存 | 本地 `/storage` | S3 |
| 7 | 溯源 / 作業庫 | SQLite + 指標 | NoCodeBackend 鏡像 |

**所有雲端整合皆可選、可優雅回落**：任一金鑰失效，自動回到免費路徑，不中斷生產。

---

## 3. 壽司博士品牌預設（已實作）

將頻道憲法 v1.0 編碼進 `src/brand.py`，管線每次生成都自動遵守：

- **視覺識別**：深藍 `#10243f`（思考/專業）＋ 暖金 `#c9a24b`（智慧/善意）＋ 米白
  `#f3ede1`（人性/留白）＋ 綠 `#3c6e47`（生命/永續）。
- **片頭台詞**：自動產生「大家好，我是壽司博士……」深藍→暖金開場 slate。
- **腳本 DNA**：貼上 `【場景】【衝突】【洞察】【方法】【反思】`，自動一拍一鏡；
  每段映射到專屬品牌漸層（場景=深藍/暖金、衝突=冷藍、方法=綠、反思=暖金餘韻…）。
- **AI 邊界（憲法）**：思想、經驗、價值判斷與最終責任來自人；AI 負責研究/初稿/視覺/剪輯/分發。
- **禁用的 AI 視覺**：藍紫霓虹、機器人大腦、漂浮數據、無意義商務畫面、過量未來科技動畫——全部寫死封禁。
- **欄目註冊表**：10 條產品線 + 「壽司切片」60 秒短影音，含航道/角色/頻率。
- **首季母題**：6 個種子題（ESG 報告寫完然後呢、AI 正在學做人…），每題附主持人原創判斷。

---

## 4. 怎麼用（三種入口）

**A. Web UI** — 開 `http://localhost:8000`，貼腳本 → 一鍵生成 → 看進度與成片。

**B. REST API**
```
POST /api/jobs            → 提交，立即回 { job_id, status:"queued" }
GET  /api/jobs/{id}       → 輪詢狀態
GET  /api/jobs/{id}/video → 成片（done 才有效）
GET  /api/health          → 健康 + feature 旗標
GET  /api/metrics         → 生產線指標（成功率/平均渲染/品牌分布）
GET  /api/series /api/brand → 欄目與品牌預設
POST /webhook/n8n         → n8n 同步回傳 { job_id, status, ok, video_url, shots, error }
```

**C. n8n 自動化** — HTTP Request 節點 POST 到 `/webhook/n8n`，
body `{ "title", "script", "brand_preset":"sushi_dr" }`，回傳含 `ok` 旗標（成功出片才 True），
可直接接排程 / 社群分發。

---

## 5. 安全與可靠性（已通過 28 測試 + CI）

- **Webhook 認證**：`WEBHOOK_SECRET` 啟用後以 `X-AI-Station-Key` header 或 `?key=` 校驗，
  使用 `hmac.compare_digest` 常數時間比對（防時序側信道）。
- **路徑穿越防護**：`/storage/{path}` 與 `/video` resolve 後確認在儲存區內。
- **背景作業不卡死**：渲染失敗寫入 `failed` + 紀錄錯誤，不再永遠卡在 `queued`。
- **雲端優雅回落**：Runway / OpenAI 失敗自動回免費路徑。
- **結構化日誌**：關鍵階段與失敗 traceback 皆有 log，層級可調。

---

## 6. 部署狀態（已上線）

| 項目 | 狀態 |
|------|------|
| 程式碼 | GitHub `DingJun1028/aistation`（main，CI 全綠） |
| 容器映像 | `docker.io/dingjunhong1028/aistation:latest`（已推送，digest `sha256:98018967acf7`） |
| 測試 | 28 passed（含真 ffmpeg E2E 渲染、webhook 認證、路徑防護） |
| 本機運行 | `pip install -e . && ai-station` → `http://localhost:8000` |
| Docker 運行 | `docker run -p 8000:8000 dingjunhong1028/aistation:latest` |

---

## 7. 提案項目與優先序

| 優先 | 項目 | 說明 | 阻礙 |
|------|------|------|------|
| P0 | **試營運 3 支樣片** | 用首季母題生成 3 支 DNA 影片，人工審品牌一致性 | 無（立即可做） |
| P0 | **VPS 部署 + 網域** | docker-compose + nginx + HTTPS，對外開放製作台 | 需 VPS/網域（你提供） |
| P1 | **n8n 排程上線** | 定時觸發週更「壽司切片」+ 雙週長片 | 無 |
| P1 | **ElevenLabs 語音** | 提升配音質感（需 `ELEVENLABS_API_KEY`） | 金鑰 |
| P2 | **Runway B-roll 實測** | 真實 AI 視覺（需 `RUNWAY_API_KEY`） | 金鑰 |
| P2 | **S3 發布 + 指標盤** | 公開托管 + 產能看板（需 `AWS_*` / `NCBDB_*`） | 金鑰 |

---

## 8. 結語

AI Station 不是要取代壽司博士的判斷，而是把「重複且可被協作的工序」交給 AI，
讓主持人把時間還給**原創判斷、方法與人性的餘韻**——正是頻道憲法要求的核心。

**下一步建議**：先用 P0 試營運 3 支樣片，確認品牌調性，再推上 VPS 對外。

---
*本提案卡對應 `README.md` 技術詳述；品牌預設實作見 `src/brand.py`。*
