fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
edge-tts>=6.1.0
Pillow>=10.0.0
numpy>=1.26.0
httpx>=0.27.0
pydantic>=2.9.0
# Optional cloud extras (only required when the matching key is set):
#   pip install ".[s3]"   # boto3, for S3 publishing

